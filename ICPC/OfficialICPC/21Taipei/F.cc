#include <bits/stdc++.h>
using namespace std;
using ll = long long;
struct rec{
  int x1,x2,y1,y2,c;
  rec(int x1,int x2,int y1,int y2,int c):x1(x1),
  x2(x2),y1(y1),y2(y2),c(c){
    
  }
}
auto solve() {
  ll n;
  cin >> n;
  
}
auto main() -> int {
  ios::sync_with_stdio(false);
  cin.tie(nullptr), cout.tie(nullptr);
  int _ = 1;
  //cin >> _;
  while (_--) {
    solve();
  }
}