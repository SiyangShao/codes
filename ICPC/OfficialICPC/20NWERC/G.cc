#include <bits/stdc++.h>
using namespace std;
using ll = long long;
using ld = long double;
auto solve() {
  ll n, r, m;
  cin >> n >> r >> m;
  vector<tuple<ll, ld, ll>> trick(m);
  for (auto &[t, p, d] : trick) {
    cin >> t >> p >> d;
  }
}
auto main() -> int {
  ios::sync_with_stdio(false);
  cin.tie(nullptr), cout.tie(nullptr);
  int _ = 1;
  // cin >> _;
  while (_--) {
    solve();
  }
}