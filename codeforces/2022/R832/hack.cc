#include <bits/stdc++.h>
using namespace std;
using ll = long long;
auto main() -> int {
  cout << int(2e5) << " " << int(2e5) << endl;
  for (int i = 0; i < (2e5); ++i) {
    cout << 1 << " \n"[i + 1 == (2e5)];
  }
  for (int i = 0; i < (2e5); ++i) {
    cout << "1 " << (2e5) << endl;
  }
}