#include <bits/stdc++.h>
using namespace std;
using ll = long long;
auto solve() {
  int n;
  string s, r;
  cin >> n >> s >> r;
}
int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr), cout.tie(nullptr);
  int t;
  cin >> t;
  while (t--) {
    solve();
  }
}
